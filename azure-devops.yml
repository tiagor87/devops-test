name: $(prefix)$(version)

variables:
  major: 0
  minor: 0
  patch: $[counter(format('{0}.{1}-{2}', variables['major'], variables['minor'], variables['Build.SourceBranchName']), 0)]
  ${{if eq(variables['Build.Reason'], 'PullRequest')}}:
    prefix: $[format('pr-{0}_v', variables['System.PullRequest.PullRequestNumber'])]
    version: $[format('{1}.{2}.{3}', variables['major'], variables['minor'], variables['patch'])]
  ${{if and(ne(variables['Build.Reason'], 'PullRequest'), eq(variables['Build.SourceBranchName'], 'master'))}}:
    prefix: 'v'
    version: $[format('{0}.{1}.{2}', variables['major'], variables['minor'], variables['patch'])]
  ${{if and(ne(variables['Build.Reason'], 'PullRequest'), ne(variables['Build.SourceBranchName'], 'master'))}}:
    prefix: 'manual_v'
    version: $[format('{0}.{1}.{2}', variables['major'], variables['minor'], variables['patch'])]

trigger:
  batch: true
  branches:
    include:
      - 'master'
pr:
  autoCancel: false
  branches:
    include:
      - 'master'

stages:
  - stage:
    displayName: Build
    jobs:  
    - job:
      displayName: Build
      steps:
      - script: echo Hello, world 4!